@{
    ViewData["Title"] = "Help";
}

<h2>Help & FAQ</h2>

<h5>How do I vote in a poll?</h5>
<p>This demo shows sample polls but voting isn’t persisted yet. We can add voting + DB storage later.</p>

<h5>How do I send feedback?</h5>
<p>Open the <a href="/Feedback">Feedback</a> page and submit the form.</p>

<h5>Where is the data stored?</h5>
<p>Data is stored in memory only for this demo. To persist feedback and votes, add EF Core (SQLite or SQL Server) and configure a DbContext.</p>

<p>If you want, I can add persistence and an admin UI next.</p>
